共同点：notify()&notifyall()均能唤醒正在等待的线程，并且均是最后只有一个线程获取资源对象的锁。
不同点：notify()只能唤醒一个线程，而notifyall能够唤醒所有的线程，当线程被唤醒以后，所有被唤醒的线程竞争获取资源对象的锁，
      其中只有一个能够得到对象锁，执行代码。
      注意：wait()方法并不是在等待资源的锁，而是在等待被唤醒（notify）一旦被唤醒后，被唤醒的线程就具备了资源锁，因为无需竞争，直至再次执行wait方法，
      或者synchronized代码块执行完毕。
